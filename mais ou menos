let nota = Number(prompt(digite a primeira nota));

let=nota=Number(prompt(digite a segunda nota));



let=nota=number(prompt(digite a terceira nota));
let  = Number(prompt("digite a nota") );


soma(nota1+nota2+nota3/3);

for (contador=0,cantado=>indise contador++){
}
if {condicao media
aprovado}
reprovado
 


     
     (comtador=0,contador=>indise++)
     console.log(nota[indice])
  
